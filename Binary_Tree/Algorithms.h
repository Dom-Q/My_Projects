using std::vector;
// Merges 2 sorted vectors into a single sorted vector
void Merge_Arrays(vector<int> &final, vector<int> &left, vector<int> &right);
// Mergesort algorithm for sorting vector
void MergeSort(vector<int> &data);
// Removes duplicate values from a vector
void remove_duplicates(vector<int> &data);
